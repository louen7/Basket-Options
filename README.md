# Basket-Options
This quantitative finance project prices two-asset arithmetic basket options in a 2D Black-Scholes model. Since no closed-form formula exists , it implements a log-normal moment-matching approximation and benchmarks it against Monte Carlo simulations optimized with variance reduction (conditioning and control variates).
